import 'package:flutter/material.dart';

class MapControlledScreen extends StatelessWidget {
  const MapControlledScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Map Controlled Screen')));
  }
}
